# Canvas縦横比・キャッシュ修正版

GitHub上で次の2ファイルを丸ごと差し替えてください。

- `style.css`
- `index.html`

修正内容:

- Canvasの `width:100%` と `height:100%` の同時指定を廃止
- 960×560（12:7）の縦横比を維持
- 枠内中央へ配置
- 画面幅が狭い場合は横幅基準へ自動切替
- `style.css?v=20260711-3` として読み込み、GitHub Pagesやブラウザの古いCSSキャッシュを回避

差し替え後は通常の再読み込みで反映されます。
